"""Pure analytical computations over stored records.

Everything in this package is side-effect free: no I/O, no clock, no network.
Functions take plain values and return plain values, so they can be tested
directly against hand-built fixtures.
"""
